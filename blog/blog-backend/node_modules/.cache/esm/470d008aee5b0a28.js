let Koa,Router,bodyParser,mongoose,createFakeData,api,jwtMiddleware;_ee5‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_ee5‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_ee5‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_ee5‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_ee5‍.w("./createFakeData",[["default",["createFakeData"],function(v){createFakeData=v}]]);_ee5‍.w("./api",[["default",["api"],function(v){api=v}]]);_ee5‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();






// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
    //createFakeData();
  })
  .catch((e) => {
    _ee5‍.g.console.log('★', e);
  });

const app = new Koa();
const router = new Router();



// api 라우트 적용
router.use('/api', api.routes()); // => /api/test

// 라우터 적용 전에 bodyparser 사용하기
app.use(bodyParser());

// 라우터 적용 전에 jwMiddleware를 적용하기
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되지 않았다면, 4000을 사용
const port = 4000;
app.listen(port, () => {
  _ee5‍.g.console.log('Listening to port %d', port);
});
